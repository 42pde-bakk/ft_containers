name: Check_MacOS compilation C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: ./run.sh
      run: |
        echo "A initial message"
        uname
        ./run.sh
